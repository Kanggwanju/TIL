# 🗓️ 2025년 12월 22일 TIL - Java 다형성(Polymorphism) & 캐스팅

## 📌 오늘의 키워드

`다형성(Polymorphism)` `업캐스팅(Upcasting)` `동적 바인딩(Dynamic Binding)`
`다운캐스팅(Downcasting)` `instanceof` `매개변수 다형성` `리턴 타입 다형성`

---

## 1️⃣ 다형성(Polymorphism)이란?

### 💡 한 줄 정의

**“부모 타입 변수로 여러 자식 객체를 다루고, 호출 순간에 ‘진짜 객체’ 기준으로 동작이 결정되는 것”**

오늘 코드에서는 `Idol`(부모) 타입으로 `SoloArtist`, `GroupIdol`(자식)을 한 배열에 담고,
반복문에서 `perform()`을 호출했을 때 각각 자식 버전이 실행되는 걸 확인했다.

📌 비유로 기억하기

* `Idol` = **공연자(공통 규격)**
* `SoloArtist/GroupIdol` = **솔로 무대/그룹 무대(구체적인 공연 방식)**

---

## 2️⃣ 업캐스팅(Upcasting): “부모 타입으로 담기”

업캐스팅은 **자식 객체를 부모 타입 변수에 담는 것**이고, **자동 형변환**된다.

예)

* `Idol iu = new SoloArtist(...)`
* `Idol chaewon = new GroupIdol(...)`

✅ 장점

* 여러 자식 객체를 한 배열/리스트로 묶어서 공통 처리 가능
  (`Idol[] idols = {...}`)

한계(중요!)

* 부모 타입 변수로는 **자식 고유 기능**에 접근 불가
  (`singSolo()`, `groupDance()` 같은 것들)

---

## 3️⃣ 동적 바인딩(Dynamic Binding): “호출 순간에 진짜 타입으로 실행”

핵심:

```java
for (Idol idol : idols) {
    idol.perform();
}
```

겉보기에는 `Idol.perform()`을 호출한 것처럼 보이지만,
실제 실행은 **객체의 진짜 타입(SoloArtist/GroupIdol)의 perform()**이 실행된다.

✅ 결론

* 부모 타입으로 묶어도, 오버라이딩된 메서드는 자식 것이 실행된다.

---

## 4️⃣ 다운캐스팅(Downcasting) + instanceof: “자식 기능이 필요할 때만 꺼내 쓰기”

업캐스팅 때문에 자식 고유 기능을 못 쓰니까, 필요할 때만 **부모 → 자식으로 되돌리는 것**이 다운캐스팅

그런데, 타입이 다르면 런타임에 `ClassCastException`이 터질 수 있어서,
안전장치로 `instanceof`를 먼저 체크하는 패턴을 사용

예) `iu`가 진짜 `SoloArtist`일 때만 `singSolo()` 실행

```java
if (iu instanceof SoloArtist) {
    SoloArtist castIu = (SoloArtist) iu;
    castIu.singSolo();
}
```

---

## 5️⃣ 매개변수 다형성

`ConcertManager.startConcert(Idol idol)`처럼 **매개변수를 부모 타입으로 받으면**,
솔로든 그룹이든 전부 같은 메서드로 처리 가능

```java
public void startConcert(Idol idol) {
    System.out.println("공연을 시작합니다!");
    idol.introduce();
    idol.perform();
    System.out.println("공연이 끝났습니다.");
}
```

---

## 6️⃣ 리턴 타입 다형성

`createIdol()`은 리턴 타입이 `Idol`인데, 조건에 따라 실제로는 `SoloArtist` 또는 `GroupIdol`을 만들어서 반환

```java
public Idol createIdol(String idolName, String type) {
    if ("solo".equals(type)) {
        return new SoloArtist(idolName, "미정");
    } else if ("group".equals(type)) {
        return new GroupIdol(idolName, "미정", 0);
    }
    return null;
}
```

✅ 포인트

* 반환 타입을 부모로 잡아두면, 호출 쪽 코드를 고정해두고 내부 구현(자식 생성)을 유연하게 바꿀 수 있다.

---

## 🎯 최종 정리

| 개념         | 한 줄 요약                            |
| ---------- | --------------------------------- |
| 업캐스팅       | 자식 객체를 부모 타입에 담기(자동)              |
| 동적 바인딩     | 오버라이딩 메서드는 실행 시점에 ‘진짜 객체’ 기준으로 호출 |
| 다운캐스팅      | 부모 타입을 자식 타입으로 되돌리기(명시적)          |
| instanceof | 다운캐스팅 전 안전 확인(타입 다르면 예외 방지)       |
| 매개변수 다형성   | 메서드 파라미터를 부모로 받아서 한 번에 처리         |
| 리턴 타입 다형성  | 리턴은 부모 타입이지만 실제 반환은 자식 객체 가능      |

---

## 💻 실습 코드

### `Main.java`

```java
package chap52.poly;

import chap51.inherit.*;

public class Main {

    public static void main(String[] args) {

        // 부모의 타입을 사용할 수 있음, 모든 클래스의 부모는 Object
        // 업캐스팅(자식 타입의 객체를 부모 타입의 변수에 저장, 자동으로 형변환됨
        SoloArtist iu = new SoloArtist("아이유", "분홍신");
        GroupIdol chaewon = new GroupIdol("김채원", "르세라핌", 5);
        SoloArtist jongguk = new SoloArtist("김종국", "한남자");
        GroupIdol sakura = new GroupIdol("사쿠라", "르세라핌", 5);

        Idol[] idols = {iu, chaewon, jongguk, sakura};

        for (Idol idol : idols) {
            // 동적 바인딩: 메서드가 호출 되었을 때 실제 객체의 타입을 확인하여
            //              해당 클래스의 메서드를 실행
            idol.perform();
        }

//        SoloArtist[] idols1 = {iu, jongguk, new SoloArtist("Jennie", "Mantra")};
//        for (SoloArtist soloArtist : idols1) {
//            soloArtist.perform();
//        }
//
//        GroupIdol[] idols2 = {chaewon, sakura};
//        for (GroupIdol groupIdol : idols2) {
//            groupIdol.perform();
//        }

        System.out.println("=========================");

        ConcertManager cm = new ConcertManager();
        cm.startConcert(jongguk);

    }
}
```

### `ConcertManager.java`

```java
package chap52.poly;

import chap51.inherit.*;

public class ConcertManager {

    // 매개변수 다형성
    public void startConcert(Idol idol) {
        System.out.println("공연을 시작합니다!");
        idol.introduce();
        idol.perform();
        System.out.println("공연이 끝났습니다.");
    }

    // 리턴 타입 다형성
    public Idol createIdol(String idolName, String type) {
        if ("solo".equals(type)) {
            return new SoloArtist(idolName, "미정");
        } else if ("group".equals(type)) {
            return new GroupIdol(idolName, "미정", 0);
        }
        return null;
    }
}
```

### `Main2.java`

```java
package chap52.poly;

import chap51.inherit.*;

public class Main2 {

    public static void main(String[] args) {

        Idol iu = new SoloArtist("아이유", "분홍신");
        Idol chaewon = new GroupIdol("김채원", "르세라핌", 5);

        iu.introduce();
        iu.perform();

        // 업캐스팅의 한계, 부모 타입 변수로는 자식 클래스만의 고유한 멤버에 접근 불가
        // 다운캐스팅: 부모 타입의 변수를 자식 타입으로 변환, 명시적 캐스팅 필요
        //             잘못된 타입 변환시 에러 발생 (안전하게 instanceof 사용)
        if (iu instanceof SoloArtist) {
            SoloArtist castIu = (SoloArtist) iu;
            castIu.singSolo();
        }

        System.out.println("==================");

        chaewon.introduce();
        chaewon.perform();

        if (chaewon instanceof GroupIdol) {
            GroupIdol castChaewon = (GroupIdol) chaewon;
            castChaewon.groupDance();
        }

    }
}
```

---

## 💬 오늘의 소감

* `Idol[]`에 자식들을 다 담아놓고 `perform()`만 호출했는데도 **각자 다르게 실행**되어서 다형성을 이용하는게 정말 편리하다고 느껴졌다.
* 다운캐스팅은 “필요할 때만 조심해서” 쓰는 게 핵심이고, `instanceof`가 안전벨트 역할을 해준다.
