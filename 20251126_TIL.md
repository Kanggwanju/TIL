# 🗓️ 2025년 11월 26일 TIL - C언어

## 📌 오늘의 키워드

`연결 리스트 (Linked List)` `노드(Node)` `포인터`
`이중 포인터(Double Pointer)` `삽입(Insertion)` `메모리 해제(free)`

---

## 1️⃣ 연결 리스트의 개념

### 💡 정의

* **연결 리스트(Linked List)** 는 데이터를 저장할 때 **연속된 메모리 공간이 아닌, 흩어진 메모리를 포인터로 연결한 구조**이다.
* 각 데이터는 **노드(Node)** 단위로 저장되며,
  각 노드는

  * `데이터(data)`
  * `다음 노드를 가리키는 포인터(next)`
    로 구성된다.

### 구조체 예시

```c
typedef struct Node {
    int data;
    struct Node* next;
} Node;
```



### ✅ 특징

| 항목    | 설명                            |
| ----- | ----------------------------- |
| 저장 위치 | 메모리 내 임의의 위치 (malloc으로 동적 생성) |
| 접근 방식 | 순차 접근 (Head부터 탐색해야 함)         |
| 삽입/삭제 | 배열보다 효율적 (O(1) ~ O(N))        |
| 단점    | 임의 접근 불가능, 포인터 관리 필요          |

---

## 2️⃣ 노드 생성 및 연결

### 💻 예시 코드

```c
Node* new_node1 = (Node*)malloc(sizeof(Node));
new_node1->data = 10;
new_node1->next = NULL;

Node* new_node2 = (Node*)malloc(sizeof(Node));
new_node2->data = 20;
new_node2->next = NULL;

// 연결
new_node1->next = new_node2;

printf("첫 번째 노드 데이터: %d\n", new_node1->data);
printf("두 번째 노드 데이터: %d\n", new_node1->next->data);
```



### 🔍 결과

```
첫 번째 노드 데이터: 10  
두 번째 노드 데이터: 20
```

* 각 노드는 **malloc()** 을 통해 동적으로 생성됨
* `next` 포인터로 다음 노드를 가리키며 리스트가 완성됨
* 마지막 노드의 `next`는 **NULL** 로 표시

---

## 3️⃣ 연결 리스트의 삽입 함수

### (1) 맨 앞 삽입 — `insertAtBeginning()`

```c
void insertAtBeginning(Node** head, int new_data) {
    Node* newNode = (Node*)malloc(sizeof(Node));
    newNode->data = new_data;
    newNode->next = *head; // 기존 헤드 연결
    *head = newNode;       // 새 노드를 헤드로 지정
}
```



**포인트**:

* `Node** head` → 이중 포인터 사용 이유는
  함수 안에서 원본 head 포인터를 수정하기 위해서임.

---

### (2) 맨 뒤 삽입 — `insertAtEnd()`

```c
void insertAtEnd(Node** head, int new_data) {
    Node* newNode = (Node*)malloc(sizeof(Node));
    newNode->data = new_data;
    newNode->next = NULL;

    if (*head == NULL) {
        *head = newNode;
        return;
    }

    Node* last = *head;
    while (last->next != NULL) {
        last = last->next; // 마지막 노드까지 탐색
    }
    last->next = newNode; // 마지막에 새 노드 연결
}
```



---

## 4️⃣ 리스트 출력 및 메모리 해제

### 💻 전체 출력 함수 — `printList()`

```c
void printList(Node* node) {
    while (node != NULL) {
        printf("%d -> ", node->data);
        node = node->next;
    }
    printf("NULL\n");
}
```

---

### 💻 메모리 해제 함수 — `freeList()`

```c
void freeList(Node** head) {
    Node* current = *head;
    Node* next_node;

    while (current != NULL) {
        next_node = current->next;
        free(current);
        current = next_node;
    }
    *head = NULL;
}
```



> ✅ `free()` 후에는 `head`를 반드시 `NULL`로 초기화해
> "더 이상 연결된 노드가 없음"을 표시해야 함.

---

## 5️⃣ 전체 코드 실행 흐름

### 실행 순서 요약

1️⃣ Head = NULL
2️⃣ 맨 앞 삽입: 10, 5, 8
3️⃣ 맨 뒤 삽입: 999, 9777, 9889
4️⃣ 다시 맨 앞에 -78 추가
5️⃣ 전체 출력 후 `freeList()`로 메모리 해제

### 🔍 결과 출력

```
-78 -> 8 -> 5 -> 10 -> 999 -> 9777 -> 9889 ->
```

---

## 🎯 최종 정리

| 항목     | 설명                        |
| ------ | ------------------------- |
| 구조체 정의 | `data + next` 로 구성된 노드 단위 |
| 삽입 방식  | 앞/뒤 삽입 시 포인터 연결 조정        |
| 이중 포인터 | 함수 내부에서 head 수정 시 필수      |
| 탐색 속도  | O(N) (순차 탐색 필요)           |
| 장점     | 삽입·삭제 용이, 메모리 낭비 없음       |
| 단점     | 느린 탐색, 포인터 관리 복잡          |

---

## 💬 오늘의 소감

오늘은 **연결 리스트의 기본 구조와 삽입 로직**을 실습했다.
특히 `Node**` 형태의 이중 포인터 개념을 이해하며,
함수 내부에서 원본 포인터를 조작할 수 있다는 점이 인상 깊었다.
배열보다 삽입·삭제가 훨씬 유연하지만,
탐색 속도가 느리고 포인터 실수가 버그로 이어질 수 있음을 직접 느꼈다.
