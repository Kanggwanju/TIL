# 🗓️ 2025년 11월 27일 TIL - C언어

## 📌 오늘의 키워드

`이중 연결 리스트 (Doubly Linked List)` `노드(Node)`
`prev 포인터` `양방향 탐색` `삽입(Insertion)` `삭제(Deletion)`

---

## 1️⃣ 이중 연결 리스트(Doubly Linked List)란?

### 💡 개념 정리

* **단일 연결 리스트(Singly Linked List)** 는 노드가 `data`와 `next`만 가지고 있어 **한 방향으로만 탐색** 가능하다.
* 반면, **이중 연결 리스트(Doubly Linked List)** 는
  각 노드가 `data`, `next`, `prev`를 모두 포함하여
  **양방향 탐색이 가능한 자료구조**이다.

| 항목     | 단일 연결 리스트          | 이중 연결 리스트       |
| ------ | ------------------ | --------------- |
| 연결 방향  | 한쪽 방향만(next)       | 양방향(prev, next) |
| 탐색 속도  | 느림 (뒤로 이동 불가)      | 앞뒤 이동 가능        |
| 메모리 사용 | 적음                 | 많음 (prev 추가)    |
| 삭제 효율  | O(N) (이전 노드 탐색 필요) | O(1) (직접 접근 가능) |

---

## 2️⃣ 구조체 설계

### 💻 구조체 코드

```c
typedef struct Node {
    int data;
    struct Node* next;
    struct Node* prev;
} Node;
```

* `data` : 노드에 저장할 실제 값
* `next` : 다음 노드를 가리키는 포인터
* `prev` : 이전 노드를 가리키는 포인터



---

## 3️⃣ 노드 삽입 (insertAfter)

### 💻 코드

```c
void insertAfter(Node* prev_node, int new_data) {
    Node* newNode = (Node*)malloc(sizeof(Node));
    newNode->data = new_data;

    newNode->next = prev_node->next;
    newNode->prev = prev_node;

    if (newNode->next != NULL) {
        newNode->next->prev = newNode;
    }

    prev_node->next = newNode;
}
```



### 🔍 핵심 개념

1. 새 노드 생성 후, 데이터 설정
2. 기존 연결 고리를 조정

  * 새 노드의 `next` → 기존 `prev_node->next`
  * 새 노드의 `prev` → `prev_node`
3. 양쪽 노드 모두의 연결 갱신

  * `prev_node->next = newNode`
  * `next 노드->prev = newNode`

> 연결 순서를 잘못 지정하면 리스트 구조가 깨지므로,
> 항상 **next → prev 순서**로 수정해야 한다.

---

## 4️⃣ 노드 삭제 (deleteNode)

### 💻 코드

```c
void deleteNode(Node** head, Node* target_node) {
    if (*head == NULL || target_node == NULL) return;

    if (*head == target_node) {
        *head = target_node->next;
    }

    if (target_node->next != NULL) {
        target_node->next->prev = target_node->prev;
    }

    if (target_node->prev != NULL) {
        target_node->prev->next = target_node->next;
    }

    free(target_node);
}
```



### 🔍 핵심 개념

* **O(1) 시간에 삭제 가능** (이전 노드를 몰라도 삭제 가능)
* 삭제 시 세 가지 경우를 고려해야 함:
  1️⃣ **삭제 대상이 첫 노드(head)** → head 이동
  2️⃣ **삭제 대상이 마지막 노드가 아닌 경우** → `next->prev` 수정
  3️⃣ **삭제 대상이 첫 노드가 아닌 경우** → `prev->next` 수정

---

## 5️⃣ 양방향 출력

### 💻 정방향 출력

```c
void printForward(Node* node) {
    printf("정방향 출력: [ ");
    while (node != NULL) {
        printf("%d ", node->data);
        node = node->next;
    }
    printf("]\n");
}
```

### 💻 역방향 출력

```c
void printBackward(Node* head) {
    Node* tail = head;
    while (tail->next != NULL) tail = tail->next;

    printf("역방향 출력: [ ");
    while (tail != NULL) {
        printf("%d ", tail->data);
        tail = tail->prev;
    }
    printf("]\n");
}
```

---

## 🎯 최종 정리

| 기능     | 설명               | 시간 복잡도 |
| ------ | ---------------- | ------ |
| 노드 삽입  | prev/next 연결 재설정 | O(1)   |
| 노드 삭제  | 포인터 재연결 후 free   | O(1)   |
| 정방향 탐색 | head부터 순차 접근     | O(N)   |
| 역방향 탐색 | tail부터 역순 접근     | O(N)   |

---

## 💬 오늘의 소감

오늘은 **이중 연결 리스트(Doubly Linked List)** 를 학습하며
포인터 연결의 중요성을 다시 한 번 체감했다.
단일 리스트보다 구조는 복잡하지만,
**삭제 연산이 O(1)** 로 수행된다는 점이 굉장히 인상 깊었다.
`prev` 포인터 덕분에 앞뒤로 자유롭게 탐색할 수 있어
데이터의 삽입과 삭제가 훨씬 유연하다는 것을 확인했다.
