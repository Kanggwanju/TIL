# 🗓️ 2025년 12월 17일 TIL - Java 클래스/객체, 생성자, this, 오버로딩

## 📌 오늘의 키워드

`클래스(Class)` `객체(Object)` `필드(Field)` `메서드(Method)`
`생성자(Constructor)` `this` `this()` `생성자 오버로딩` `메서드 오버로딩` `배열 필드`

---

## 1️⃣ 클래스와 객체: “설계도 vs 실체”

### 💡 클래스/객체를 한 문장으로

* **클래스**: 객체를 만들기 위한 **설계도**
* **객체(인스턴스)**: 클래스로 만들어진 **실제 실체**

📌 비유로 기억하기

* 클래스 = **붕어빵 틀**, 객체 = **붕어빵**

### ✅ 오늘 확인한 포인트

* `new Idol()`로 **객체 생성**
* `wonyoung.name = "..."` 처럼 **필드에 값 저장**
* `wonyoung.introduce()` 처럼 **메서드 호출**

---

## 2️⃣ 필드(Field)와 메서드(Method)

* **필드**: 객체가 가진 **상태(데이터)**
* **메서드**: 객체가 할 수 있는 **행동(기능)**

예)

* Idol의 상태: `name`, `groupName`
* Idol의 행동: `introduce()`

---

## 3️⃣ 객체 안에 “배열 필드” 넣기

### 💡 왜 중요하지?

객체가 단순 값 하나만 들고 있는 게 아니라,
**여러 값을 묶어서(배열) 속성으로 가질 수도** 있다는 걸 확인했어.

* `Playlist` 객체가 `String[] songTitles`을 필드로 가짐
* `songTitles[1]`처럼 배열 요소 접근 가능

---

## 4️⃣ 생성자(Constructor): 객체 생성 순간 초기화

### 💡 생성자가 하는 일

* 객체가 만들어지는 순간(`new`) **필드를 초기화**하거나 **준비 작업** 수행

오늘 실습에서 본 패턴

* `Book(String title)` → 생성하자마자 제목 세팅 + 출력
* `Car(String name)` → 내부에서 다른 생성자를 호출해서 중복 제거

---

## 5️⃣ this / this(): “중복 제거”의 핵심

### ✅ this (필드 vs 파라미터 구분)

* 생성자 파라미터 `name`과 필드 `name`이 이름이 같을 때
* `this.name = name;` 처럼 **왼쪽(this.name)은 필드**, 오른쪽(name)은 파라미터

### ✅ this() (생성자 체이닝)

* 한 생성자에서 **다른 생성자를 호출**해서 공통 로직 재사용

예)

* `Idol(String name)` → `this(name, "솔로가수")`
* `Car(String name)` → `this(name, 2024)`

---

## 6️⃣ 오버로딩(Overloading)

### 1) 생성자 오버로딩

* 생성자 이름은 같아도 **파라미터가 다르면** 여러 개 만들 수 있음

### 2) 메서드 오버로딩

* `add(int, int)` / `add(double, double)` 처럼
* 이름은 같아도 **파라미터 타입/개수가 다르면** 다른 메서드로 취급

📌 핵심 한 줄

* “**이름은 같게, 사용법은 다양하게**” → 호출할 때 타입에 맞게 자동 선택됨

---

## 🎯 최종 정리

| 오늘 배운 것 | 한 줄 요약               |
| ------- | -------------------- |
| 클래스/객체  | 클래스는 설계도, 객체는 실체     |
| 필드/메서드  | 필드는 상태, 메서드는 행동      |
| 배열 필드   | 객체는 배열도 속성으로 가질 수 있음 |
| 생성자     | 객체 생성 순간 초기화 담당      |
| this    | 필드와 파라미터 구분          |
| this()  | 생성자끼리 호출해서 중복 제거     |
| 오버로딩    | 같은 이름 + 다른 파라미터로 다양화 |

---

## 💻 실습 코드

### `Idol.java`

```java
package chap48;

// 설계도 역할을 하는 클래스는 main 메서드를 만들지 않는다.
public class Idol {

    // 아이돌의 속성 : 필드
    String name;
    String groupName;

    // 아이돌의 기능 : 메서드
    void introduce() {
        System.out.println("안녕하세요! " + groupName + "의 " + name + "입니다.");
    }
}

// 실행용 클래스
class Main {

    public static void main(String[] args) {
        // 아이돌들을 만들어보자! - 객체 생성
        Idol wonyoung = new Idol();
        Idol karina = new Idol();

        wonyoung.name = "장원영";
        wonyoung.groupName = "아이브";

        karina.name = "카리나";
        karina.groupName = "에스파";

        wonyoung.introduce();
        karina.introduce();
    }

}
```

### `Quiz1.java`

```java
package chap48;

public class Quiz1 {

    public static void main(String[] args) {

        Webtoon w = new Webtoon();

        w.title = "나 혼자만 레벨업";
        w.author = "추공";
        w.rating = 9.92;

        w.printInfo();
    }
}

class Webtoon {
    String title;
    String author;
    double rating;

    void printInfo() {
        System.out.printf("제목: %s (작가: %s)\n", title, author);
    }
}
```

### `Quiz2.java`

```java
package chap48;

public class Quiz2 {

    public static void main(String[] args) {

        Playlist p = new Playlist();

        p.owner = "판주";
        p.songTitles = new String[]{"Dynamite", "Hype Boy", "Ditto"};

        System.out.println(p.songTitles[1]);
    }
}

class Playlist {
    String owner;
    String[] songTitles;
}
```

### `Idol.java`

```java
package chap49;

public class Idol {

    String name;
    String groupName;

    // 기본 생성자
    Idol() {
    }

    // name과 groupName을 초기화하는 생성자
    Idol(String name, String groupName) {
        this.name = name;
        this.groupName = groupName;
    }

    // name만 받아서 솔로가수라는 이름으로 생성
    Idol(String name) {
        this(name, "솔로가수"); // 생성자 위임
    }

    void introduce() {
        System.out.println("안녕하세요! " + groupName + "의 " + name + "입니다.");
    }

    // 오버로딩: 초기 메시지를 받고 소개
    void introduce(String initMessage) {
        System.out.println(initMessage);
        this.introduce();
    }

}

class Main {

    public static void main(String[] args) {

        Idol wonyoung = new Idol("장원영", "아이브");
        wonyoung.introduce();

        Idol jennie = new Idol("제니", "블랙핑크");
        jennie.introduce("하이!");
        jennie.introduce();

        Idol iu = new Idol("아이유");
        iu.introduce();
    }

}
```

### `Quiz1.java`

```java
package chap49;

public class Quiz1 {

    public static void main(String[] args) {

        Book b = new Book("백엔드 완전 정복");

    }
}

class Book {
    String title;

    Book(String title) {
        this.title = title;
        System.out.println(title + " 읽는 중...");
    }
}
```

### `Quiz2.java`

```java
package chap49;

public class Quiz2 {

    public static void main(String[] args) {

        Car c1 = new Car("아반떼");
        Car c2 = new Car("소나타", 2020);

    }
}

class Car {

    String name;
    int year;

    Car(String name) {
        this(name, 2024);
        System.out.println(name + " 출고 완료!");
    }

    Car(String name, int year) {
        this.name = name;
        this.year = year;
        System.out.println(name + " 생산 완료!");
    }

}
```

### `Quiz3.java`

```java
package chap49;

public class Quiz3 {

    public static void main(String[] args) {

        Calculator c = new Calculator();
        System.out.println(c.add(10, 20));
        System.out.println(c.add(1.5, 2.5));

    }
}

class Calculator {
    int add(int a, int b) {
        return a + b;
    }

    double add(double a, double b) {
        return a + b;
    }
}
```

---

## 💬 오늘의 소감

* 클래스/객체를 직접 만들어보니까 “**설계도-실체**” 감각이 확 잡혔다.
* 생성자 + `this()`가 특히 좋았다. **중복 코드를 줄이는 느낌**이 확실했다.
* 오버로딩은 “같은 이름으로 편하게 쓰되, 상황에 맞게 자동 선택”되는 게 핵심이라고 이해했다.
