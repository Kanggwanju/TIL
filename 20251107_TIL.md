# 🗓️ 2025년 11월 7일 TIL - c언어

## 📌 오늘의 키워드
`포인터(pointer)`, `주소(&)`, `간접참조(*)`, `포인터 크기`, `원본 수정`, `이중 포인터(**)`

---

## 1) 주소와 값의 구분 (포인터 없이 시작하기)

### 개념 정리
- 변수에는 **내용물(값)** 과 **보관 위치(주소)** 가 있다.
- `&a`는 변수 `a`의 **주소**를, `a` 자체는 **값**을 의미한다.

### 예시 코드
```c
int a = 10;
printf("a의 값 (내용물): %d\n", a);
printf("a의 주소 (번지수): %p\n", &a);
````

* 값과 주소를 각각 출력해 **메모리 관점**을 익히는 기본 예제

### 포인트

* “값”과 “주소”를 혼동하지 않기
* 주소는 보통 `%p` 서식으로 출력한다.

---

## 2) 포인터 선언과 간접참조

### 개념 정리

* `int *p;` : **정수(int)를 가리키는 포인터** 선언
* `p = &a;` : 포인터 `p`가 변수 `a`의 **주소를 저장**
* `*p` : 포인터 `p`가 가리키는 곳의 **값(간접참조)**

### 예시 코드

```c
int a = 10;
int *p;      // 포인터 변수
p = &a;

printf("a의 주소: %p\n", &a);
printf("p의 값: %p\n", p);
printf("포인터 p로 찾아간 곳의 값: %d\n", *p);
```

* 포인터가 **주소를 담는 변수**임을 확인하고, `*p`로 값을 읽는다.
* 포인터 변수의 크기는 대개 8바이트(64비트 기준)

---

## 3) 포인터로 원본 값 바꾸기 (Call by Reference 효과)

### 개념 정리

* `*p = ...;` 형태로 **가리키는 대상의 값**을 직접 변경할 수 있다.
* 값 복사(Call by Value)로는 못 하던 **원본 수정**이 가능해진다.

### 예시 코드

```c
int characterHP = 50;
int *healPtr = &characterHP;

printf("치유 마법 시전 전 HP: %d\n", characterHP);

*healPtr += 30;

printf("치유 마법 시전 후 HP: %d\n", characterHP);
```

* 포인터로 접근해 `characterHP`를 +30 시킨다(원본 변경).

### 포인트

* “원본을 바꿔야 하는 함수”라면 **포인터(또는 참조 역할)** 를 사용해 설계한다.

---

## 4) 이중 포인터(**pp)로 한 단계 더

### 개념 정리

* `int *p`는 **int의 주소**, `int **pp`는 **int*의 주소**를 담는다.
* `*p`는 값, `**pp`도 최종적으로 값에 도달

### 예시 코드

```c
int a = 30;
int *p = &a;
int **pp = &p;

printf("1. 일반변수로 접근: a = %d\n", a);
printf("2. 포인터변수로 접근: p = %d\n", *p);
printf("3. 이중 포인터변수로 접근: pp = %d\n", **pp);

printf("1. a의 주소: %p\n", &a);
printf("2. p에 저장된 값: %p\n", p);
printf("3. p의 주소: %p\n", &p);
printf("4. pp에 저장된 값: %p\n", pp);

**pp = 500;   // a를 500으로 변경
printf("a = %d\n", a);
```

* `**pp = 500;`으로 **최종 대상(a)** 를 바꿀 수 있음을 보여준다.

### 포인트

* 다단 포인터는 **간접참조 수준**이 늘어나므로,
  “주소의 주소” 관계를 그림으로 먼저 정리하면 헷갈림을 줄일 수 있다.

---

## 🎯 최종 정리

* 포인터는 “**주소를 저장하는 변수**”이며, `*p`로 **가리킨 값**에 접근
* 포인터를 이용하면 **원본 데이터 수정**이 가능해져, 함수 인자 전달 시 유용
* 이중 포인터는 “주소의 주소”를 다루며, `**pp`로 최종 값에 도달해 변경까지 가능
* 값과 주소를 구분해 출력하는 기본 예제로 **메모리 관점**을 먼저 익히는 것이 중요

---

## 💬 오늘의 소감

포인터를 “값을 담는 상자”가 아닌 “**상자의 위치(주소)를 담는 메모**”라고 이해하니 훨씬 명확해졌다.
