# 🗓️ 2025년 12월 11일 TIL – C언어 기출 15문제 풀이

## 📌 오늘의 키워드

`기출 문제 풀이` `배열과 포인터` `문자열` `버블 정렬` `재귀·팩토리얼`
`포인터 배열` `구조체 포인터` `문자열 길이 함수`
`소수 판별` `숫자 뒤집기` `거듭제곱`

---

## 1️⃣ 버블 정렬 – 2020년 1회차

### 💻 코드

```c
#include <stdio.h>

int main(void) {
    
    int i, j;
    int temp;
    int a[5] = {75, 95, 85, 100, 50};

    // 버블 정렬 알고리즘: 인접한 두 요소를 비교하여 큰 값을 오른쪽으로 이동
    for (i = 0; i < 4; i++) {
        for (j = 0; j < 4 - i; j++) {
            if (a[j] > a[j + 1]) {
                // Swap 알고리즘
                temp = a[j];
                a[j] = a[j + 1];
                a[j + 1] = temp;
            }
        }
    }

    // 정답: 50 75 85 95 100
    for (i = 0; i < 5; i++) {
        printf("%d ", a[i]);
    }

    return 0;
}
```

### 🌱 핵심 개념

* **버블 정렬**: 인접한 두 값을 비교 → 큰 값을 오른쪽으로 보내면서 정렬
* 시간 복잡도는 O(N²).

### 🔁 코드 흐름 & 결과

* 두 겹 for문으로 총 4번 패스
* 각 패스마다 뒤쪽으로 큰 수가 “버블”처럼 밀려난다.
* 최종 출력:
  `50 75 85 95 100`

> 🧩 한 줄 정리: **인접한 두 수를 계속 바꿔가며 오른쪽 끝부터 정렬되는 방식**

---

## 2️⃣ 함수 호출 체인 – 2020년 3회차

### 💻 코드

```c
#include <stdio.h>

int r1() { return 4; }

int r10() { return 30 + r1(); }

int r100() { return 200 + r10(); }

int main(void) {
    /*
        r1() = 4
        r10() = 30 + 4 = 34
        r100() = 200 + 34 = 234
        정답: 234
    */
    printf("%d\n", r100());
    return 0;
}
```

### 🌱 핵심 개념

* 작은 함수들을 **조합해서 값을 만드는 패턴**
* 시험에서는 “호출 순서와 최종 값”을 묻기 좋다

### 🔁 흐름 & 결과

* `r1()` → 4
* `r10()` → 30 + 4 = 34
* `r100()` → 200 + 34 = 234
* 출력: `234`

---

## 3️⃣ 반복문으로 팩토리얼 – 2020년 3회차

### 💻 코드

```c
#include <stdio.h>

int main(void) {
    
    int i = 0, c = 1;
    while (i < 10) {
        i++;
        c *= i;
    }
    // 정답: 10 팩토리얼 = 1 * 2 * ... * 10 = 3628800
    printf("%d", c);

    return 0;
}
```

### 🌱 핵심 개념

* while문으로 1부터 10까지 곱하면 **10!** 계산
* 누적 곱 패턴 익히기 좋음

### 🔁 결과

* 출력: `3628800`

---

## 4️⃣ 포인터와 문자열 – 2020년 4회차

### 💻 코드

```c
#include <stdio.h>

int main(void) {
    
    // 문자열 K O R E A \0
    char *p = "KOREA";

    // %s는 \0을 만날때까지 반복 출력
    printf("%s\n", p);        // KOREA
    printf("%s\n", p + 3);    // EA
    
    // %c는 한 글자만 출력
    printf("%c\n", *p);       // K
    printf("%c\n", *(p + 3)); // E
    printf("%c\n", *(p + 2)); // R

    return 0;
}
```

### 🌱 핵심 개념

* 문자열 리터럴은 `char *`로 받으면 “시작 주소”를 가리킴
* `p + 3` → 3칸 뒤 주소, 즉 `"EA"`의 시작 위치

### 🔁 결과

```
KOREA
EA
K
E
R
```

> 한 줄 정리: **문자열 + 포인터 덧셈 = 뒷부분 문자열 슬라이싱처럼 동작**

---

## 5️⃣ 구조체 배열과 포인터 – 2021년 1회차

### 💻 코드

```c
#include <stdio.h>

// 구조체
struct good {
    char name[10];
    int age;
};

int main(void) {
    
    struct good s[] = {
        "Kim", 28,  // [0]
        "Lee", 38,  // [1]
        "Seo", 50,  // [2]
        "Park", 35  // [3]
    };

    // 구조체 포인터
    struct good *p;
    // p는 s의 0번 인덱스를 바라봄
    p = s;
    p++; // 1번 인덱스를 바라봄

    printf("%s\n", p->name); // Lee
    printf("%d\n", p->age);  // 38

    return 0;
}
```

### 🌱 핵심 개념

* `p = s;` → 배열 첫 요소 주소
* `p++` → **구조체 크기만큼** 이동 → 다음 학생

### 🔁 결과

```
Lee
38
```

---

## 6️⃣ 배열과 포인터 혼합 – 2021년 2회차

### 💻 코드

```c
#include <stdio.h>

int main(void) {
    
    int arr[3];
    int s = 0;

    // arr[0] = 1
    *(arr + 0) = 1;

    // arr[1] = arr[0] + 2 = 1 + 2 = 3
    arr[1] = *(arr + 0) + 2;

    // arr[2] = arr[0] + 3 = 1 + 3 = 4
    arr[2] = *arr + 3;

    // s = 1 + 3 + 4 = 8
    for (int i = 0; i < 3; i++) {
        s = s + arr[i];
    }

    // 정답: 8
    printf("%d", s);

    return 0;
}
```

### 🌱 핵심 개념

* `arr[i]` 와 `*(arr + i)` 는 동치
* 포인터 표기와 배열 표기를 섞어서 사용하는 스타일

### 🔁 결과

* 배열 내용: `[1, 3, 4]`
* 합계: `8`

---

## 7️⃣ 함수 선언·정의·거듭제곱 – 2021년 2회차

### 💻 코드

```c
#include <stdio.h>

// 함수 선언
int mp(int base, int exp);

int main(void) {

    int res;
    res = mp(2, 10);
    // 정답: 1024
    printf("%d", res);
    return 0;
}

// 함수 정의
int mp(int base, int exp) {
    int res = 1;
    // 1 * 2 * ... * 2 = 2^10 = 1024
    for (int i = 0; i < exp; i++) {
        res = res * base;
    }
    return res;
}
```

### 🌱 핵심 개념

* 함수 선언 → main → 함수 정의 구조
* 반복문으로 `base`를 `exp`번 곱하기 → `2^10`.

### 🔁 결과

`1024`

---

## 8️⃣ 구조체 포인터 연산 – 2021년 3회차

### 💻 코드

```c
#include <stdio.h>

struct jsu {
    char name[12];
    int os, db, hab, hhab;
};

int main(void) {
    
    struct jsu st[3] = { {"데이터1", 95, 88}, {"데이터2", 84, 91}, {"데이터3", 86, 75} };
    struct jsu* p;

    // p는 st의 0번 인덱스를 바라봄.
    p = &st[0];

    //                84       +     75      = 159
    (p + 1)->hab = (p + 1)->os + (p + 2)->db;

    //                  159      +  95   +  88   = 342
    (p + 1)->hhab = (p + 1)->hab + p->os + p->db;

    //                 159      +      342       = 501
    printf("%d\n", (p + 1)->hab + (p + 1)->hhab);

    return 0;
}
```

### 🌱 핵심 개념

* `(p + 1)` → 두 번째 학생, `(p + 2)` → 세 번째 학생
* 구조체 필드 연산을 통해 합계 필드 계산

### 🔁 결과

`501`

---

## 9️⃣ 포인터 배열 – 2021년 3회차

### 💻 코드

```c
#include <stdio.h>

int main(void) {
    
    int *arr[3];
    int a = 12, b = 24, c = 36;

    arr[0] = &a;
    arr[1] = &b;
    arr[2] = &c;

    // arr[1]은 b의 주소, *arr[1]은 arr[1]이 가리키는 주소의 실제 값 = 24
    // *arr은 arr[0], a의 주소, **arr은 arr[0]이 가리키는 주소의 실제 값 = 12
    // 정답: 24 + 12 + 1 = 37
    printf("%d\n", *arr[1] + **arr + 1);

    return 0;
}
```

### 🌱 핵심 개념

* `int *arr[3];` → “포인터들의 배열”
* `*arr`는 `arr[0]` 과 같음

### 🔁 결과

`37`

---

## 🔟 소수 판별 및 최대 소인수 – 2022년 1회차

### 💻 코드

```c
#include <stdio.h>

// 소수 판별
int isPrime(int number) {
    int i;
    for (i = 2; i < number; i++) {
        if (number % i == 0) return 0;
    }
    return 1;
}

int main(void) {
    
    int number = 13195, max_div = 0, i;

    for (i = 2; i < number; i++) {
        // i가 소수이고, i가 number(13195)의 약수일 경우
        if (isPrime(i) == 1 && number % i == 0) {
            max_div = i;
        }
    }

    // 13195의 소인수들: 5, 7, 13, 29
    // 정답: 29
    printf("%d", max_div);
    return 0;
}
```

### 🌱 핵심 개념

* `isPrime(i)` 는 **i가 소수면 1, 아니면 0**을 반환하는 정상적인 소수 판별 함수
* **i가 소수**이고
* **i가 number의 약수**인 경우만 소인수로 인정

* 루프에서 가장 큰 소인수를 계속 갱신 → 최종적으로 가장 큰 소인수가 `max_div`에 저장됨


### 🔁 결과

* 13195의 소인수: 5, 7, 13, 29
* 최대 소인수: `29`

---

## 1️⃣1️⃣ 숫자 역순 변환 – 2022년 1회차

### 💻 코드

```c
#include <stdio.h>

int main(void) {
    
    int number = 1234;
    int div = 10;
    int result = 0;

    // 빈칸 1. number와 0 사이의 비교연산자 ( > 또는 != )
    while (number > 0) {
        result = result * div;
        // 빈칸 2. number와 div 사이의 연산자 ( % )
        result = result + number % div;
        // 빈칸 3. number와 div 사이의 연산자 ( / )
        number = number / div;

    }

    // 출력결과가 4321이 되게끔 만드는 빈칸 채우기
    printf("%d", result);
    return 0;
}
```

### 🌱 핵심 개념

* `number % 10` → 마지막 자리 숫자
* `result = result * 10 + 마지막 자리` 형태로 숫자를 뒤집어 생성

### 🔁 결과

* 입력으로 고정된 `1234` → 출력: `4321`

---

## 1️⃣2️⃣ 재귀 함수 & 팩토리얼 – 2022년 1회차

### 💻 코드

```c
#include <stdio.h>

int func(int a) {
    if (a <= 1) return 1;
    return a * func(a - 1); // 재귀
}

int main(void) {

    int a;

    // 결과가 120 나오는 입력값 a
    scanf("%d", &a);
    
    // 120 = func(5) = 5 * func(4) = 5 * 4 * func(3) = ... = 5 * 4 * 3 * 2 *1 = 120
    printf("%d", func(a));

    return 0;
}
```

### 🌱 핵심 개념

* 재귀 호출: `func(a)`가 자기 자신을 `a-1`로 다시 호출
* 종료 조건: `a <= 1`일 때 1 반환

### 🔁 예시

* 입력: `5` → 출력: `120`

---

## 1️⃣3️⃣ 배열과 포인터 연산 – 2022년 2회차

### 💻 코드

```c
#include <stdio.h>

int main(void) {
    
    int a[4] = {0, 2, 4, 8};
    int b[3] = {};
    int i = 1;
    int sum = 0;
    int *p1;

    /*
        p1 = a + 1 = a[1]
        b[0] = *p1 - a[0] = a[1] - a[0] = 2 - 0 = 2
        sum = sum + b[0] + a[1] = 0 + 2 + 2 = 4

        p1 = a + 2 = a[2]
        b[1] = *p1 - a[1] = a[2] - a[1] = 4 - 2 = 2
        sum = 4 + b[1] + a[2] = 4 + 2 + 4 = 10

        p1 = a + 3 = a[3]
        b[2] = *p1 - a[2] = a[3] - a[2] = 8 - 4 = 4
        sum = 10 + b[2] + a[3] = 10 + 4 + 8 = 22
    */
    for (i; i < 4; i++) {
        p1 = a + i;
        b[i - 1] = *p1 - a[i - 1];
        sum = sum + b[i - 1] + a[i];
    }

    // 정답: 22
    printf("%d", sum);

    return 0;
}
```

### 🌱 핵심 개념

* `p1 = a + i` → `a[i]` 를 가리키는 포인터
* `b[i-1]` 에는 각 원소간의 “차이” 저장

### 🔁 결과

* 최종 `sum = 22`

---

## 1️⃣4️⃣ 문자열 길이 계산 함수 – 2022년 2회차

### 💻 코드

```c
#include <stdio.h>

// 함수 선언
int len(char* p);

int main(void) {
    
    char* p1 = "2022";
    char* p2 = "202207";

    int a = len(p1); // 4
    int b = len(p2); // 6

    // 정답: 10
    printf("%d", a + b);

    return 0;
}

// 함수 정의, 문자 수 세기
int len(char* p) {
    int r = 0;
    while (*p != '\0') {
        p++;
        r++;
    }
    return r;
}
```

### 🌱 핵심 개념

* C 문자열의 끝은 항상 `'\0'`
* 포인터를 한 칸씩 증가시키며 문자 수 세기

### 🔁 결과

* `"2022"` 길이 4 + `"202207"` 길이 6 = `10`

---

## 1️⃣5️⃣ 문자열과 포인터 – 2023년 1회차

### 💻 코드

```c
#include <stdio.h>

int main(void) {
    
    // A r t \0
    char a[] = "Art";
    char* p = NULL;
    p = a;

    printf("%s\n", a);  // Art
    printf("%c\n", *p); // A
    printf("%c\n", *a); // A
    printf("%s\n", p);  // Art

    // Art
    for (int i = 0; a[i] != '\0'; i++) {
        printf("%c", a[i]);
    }

    return 0;
}
```

### 🌱 핵심 개념

* `char a[] = "Art";` 는 **배열**이고,
  `char *p = a;` 는 그 배열의 첫 번째 요소 주소를 가리킴
* `*p` 와 `*a` 는 둘 다 `'A'`

### 🔁 출력

```
Art
A
A
Art
Art
```

---

## 🎯 오늘의 최종 정리

| 범주       | 내용                                         |
| -------- | ------------------------------------------ |
| 배열 & 포인터 | `arr[i]` ↔ `*(arr + i)`, 포인터 배열, 포인터 산술 복습 |
| 문자열      | 포인터로 문자열 접근, 길이 계산 함수, `%s` vs `%c`        |
| 구조체      | 구조체 배열 + 포인터, `p++` 의 의미, `p->field` 사용법   |
| 수학 로직    | 소인수, 숫자 뒤집기, 거듭제곱, 팩토리얼(반복 & 재귀)           |
| 시험 패턴    | “빈칸 채우기(연산자/조건)” + “출력 결과 맞추기” 유형이 반복됨     |

---

## 💬 오늘의 소감

오늘은 **실제 기출 문제 코드 전체를 보면서** 복습하니까,
단순히 “답이 뭐였더라?”가 아니라
**“C에서 이런 표현을 쓰면 실제로 메모리에서는 이렇게 움직이겠구나”** 하는 감각이 더 잘 잡혔다.

특히

* 포인터 배열,
* 구조체 포인터 연산,
* 문자열 길이 함수,
* 소수 판별·숫자 뒤집기 같은 수학 로직 문제들이
  앞으로 나올 자료구조/알고리즘 코드에도 그대로 연결된다는 느낌이 들었다.
