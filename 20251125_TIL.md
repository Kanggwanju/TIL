# 🗓️ 2025년 11월 25일 TIL - C언어

## 📌 오늘의 키워드

`배열(Array)` `자료구조(Data Structure)` `ADT(추상 자료형)`
`삽입(Insertion)` `삭제(Deletion)` `시간 복잡도(Time Complexity)`

---

## 1️⃣ 자료구조와 배열의 개념

### 💡 자료구조란?

* 데이터를 효율적으로 **저장하고 사용하기 위한 구조적 방법**
* 핵심은 데이터에 **질서와 구조를 부여하는 것**
* 예: 배열, 연결 리스트, 스택, 큐, 트리, 그래프 등

### 💡 추상 자료형(ADT, Abstract Data Type)

* 자료구조를 **설계도 관점**에서 본 개념
* 내부 동작을 몰라도 **기능만 이해하면 사용할 수 있음**

  > 예: 자동차 운전은 내부 엔진 구조를 몰라도 가능

---

## 2️⃣ 배열(Array)의 특징

| 구분    | 설명                                          |
| ----- | ------------------------------------------- |
| 저장 형태 | **같은 자료형**의 데이터가 **연속된 메모리 공간**에 저장됨        |
| 장점    | 인덱스를 이용해 임의 접근이 가능 → **접근 속도 O(1)**         |
| 단점    | 크기가 **고정**되어 있으며, 중간 삽입·삭제 시 **비효율적(O(N))** |
| 위험    | 버퍼 오버플로우, 메모리 낭비, 부족 문제 발생 가능               |

---

## 3️⃣ 배열의 삽입(Insertion)

### 💻 예시 코드

```c
int scores[10] = {98, 72, 88, 91};
int count = 4;

int insert_data = 100;
int insert_idx = 2;

// 뒤에서부터 한 칸씩 밀기
for (int i = count - 1; i >= insert_idx; i--) {
    scores[i + 1] = scores[i];
}
scores[insert_idx] = insert_data;
count++;
```



### 🔍 실행 결과

```
삽입 전: 98 72 88 91
삽입 후: 98 72 100 88 91
```

### 📘 설명

* `insert_idx` 이후의 데이터를 **뒤로 한 칸씩 이동**시킨 뒤, 새로운 값을 삽입
* 시간 복잡도: **O(N)** (최악의 경우 전체 이동)

---

## 4️⃣ 배열의 삭제(Deletion)

### 💻 예시 코드

```c
int scores[10] = {98, 72, 100, 88, 91};
int count = 5;

int del_idx = 2;

// 삭제 인덱스 이후의 데이터 앞으로 이동
for (int i = del_idx; i < count - 1; i++) {
    scores[i] = scores[i + 1];
}
count--;
```



### 🔍 실행 결과

```
삭제 전: 98 72 100 88 91
삭제 후: 98 72 88 91
```

### 📘 설명

* `del_idx` 이후의 데이터를 **앞으로 한 칸씩 이동**
* 실제로 데이터를 지우는 것이 아니라, **덮어쓰기 방식으로 처리**
* 시간 복잡도: **O(N)** (최악의 경우 전체 이동)

---

## 5️⃣ 배열 삽입·삭제의 시각적 이해

| 단계 | 삽입(Insertion)     | 삭제(Deletion)    |
| -- | ----------------- | --------------- |
| 전  | 98 72 88 91       | 98 72 100 88 91 |
| 작업 | 72와 88 사이에 100 삽입 | 인덱스 2(100) 삭제   |
| 후  | 98 72 100 88 91   | 98 72 88 91     |

---

## 🎯 최종 정리

| 항목        | 설명               | 시간 복잡도 |
| --------- | ---------------- | ------ |
| **배열 접근** | 인덱스로 직접 접근 가능    | O(1)   |
| **배열 삽입** | 특정 위치에 새 데이터 추가  | O(N)   |
| **배열 삭제** | 특정 위치 데이터 제거     | O(N)   |
| **단점**    | 고정 크기, 삽입·삭제 비효율 | -      |
| **장점**    | 빠른 접근, 구현 단순     | -      |

---

## 💬 오늘의 소감

오늘은 **배열의 기초 자료구조적 관점**에서 삽입과 삭제를 학습했다.
배열은 **접근 속도는 빠르지만**, **삽입과 삭제는 비효율적**이라는 구조적 한계를 가진다.
이 점 때문에 실제로는 연결 리스트(Linked List)가 배열의 단점을 보완하기 위해 많이 사용된다는 점도 자연스럽게 이해되었다.
