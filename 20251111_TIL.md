# 🗓️ 2025년 11월 11일 TIL - c언어

## 📌 오늘의 키워드

`Call by Reference`, `포인터 매개변수`, `배열 전달`, `배열 포인터`, `포인터 배열`, `2차원 배열 함수 전달`

---

## 1️⃣ Call by Reference 개념과 구현

### 개념 정리

* **Call by Value(값에 의한 호출)** 은 변수의 값을 복사하여 전달한다.
  → 함수 내부에서 값을 변경해도 **원본에 영향이 없음.**
* **Call by Reference(참조에 의한 호출)** 은 변수의 **주소를 전달**하여,
  함수 내부에서 원본을 직접 수정할 수 있다.

### 예시 코드

```c
void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main(void) {
    int x = 10, y = 20;
    swap(&x, &y);
    printf("x: %d, y: %d\n", x, y);
    return 0;
}
```

✅ 실행 결과

```
x: 20, y: 10
```

### 핵심 포인트

* `int *a, *b` → 포인터 매개변수 선언
* `*a`, `*b` → 포인터 역참조로 실제 변수에 접근
* `swap(&x, &y)` → 주소 전달로 원본 값이 변경됨

---

## 2️⃣ 배열 전달과 Call by Reference

### 개념 정리

* 배열 이름은 **배열의 첫 번째 요소 주소**이므로,
  배열을 함수로 전달하면 **자동으로 Call by Reference 형태**가 된다.
* 함수 내부에서 배열 원소를 변경하면 원본 배열에 즉시 반영된다.

### 예시 코드

```c
void doubleElements(int arr[], int size) {
    for (int i = 0; i < size; i++) {
        arr[i] *= 2;  // 원본 배열 직접 변경
    }
}

int main(void) {
    int nums[5] = {1, 2, 3, 4, 5};
    doubleElements(nums, 5);

    for (int i = 0; i < 5; i++) {
        printf("%d ", nums[i]);
    }
    return 0;
}
```

✅ 실행 결과

```
2 4 6 8 10
```

### 핵심 포인트

* 배열 전달 시 별도의 `&` 연산 필요 없음.
* 배열의 첫 주소가 이미 전달되기 때문.
* 함수 내부 변경 → 원본 배열이 바뀜

---

## 3️⃣ 2차원 배열 전달과 배열 포인터

### 개념 정리

* 2차원 배열은 메모리에 **연속된 행(row)** 으로 저장된다.
* 단순히 `int **` 형태로는 크기 정보를 알 수 없기 때문에,
  “**한 행이 몇 열(column)** 로 구성되어 있는지”를 명시해야 한다.
* 즉, `int (*arr)[3]` 처럼 **배열 포인터**를 이용해야 한다.

### 예시 코드

```c
void print2DArray(int (*arr)[3]) {
    for (int i = 0; i < 2; i++) {
        for (int j = 0; j < 3; j++) {
            printf("%d ", arr[i][j]);
        }
        printf("\n");
    }
}

int main(void) {
    int matrix[2][3] = {
        {1, 2, 3},
        {4, 5, 6}
    };

    print2DArray(matrix);
    return 0;
}
```

✅ 실행 결과

```
1 2 3
4 5 6
```

### 핵심 포인트

* `int (*arr)[3]` → “**3개짜리 int 배열을 가리키는 포인터**”
* `int *arr[3]`과 혼동 금지 (이건 ‘포인터를 3개 담는 배열’)
* 배열 포인터는 행 단위 접근에 적합하며, `arr[i][j]` 사용 가능

---

## 🎯 최종 정리

| 구분                | 설명               | 예시                        |
| ----------------- | ---------------- | ------------------------- |
| Call by Value     | 값 복사 전달 → 원본 불변  | `swap(x, y)`              |
| Call by Reference | 주소 전달 → 원본 수정    | `swap(&x, &y)`            |
| 배열 전달             | 배열 이름 자체가 주소     | `doubleElements(nums, 5)` |
| 배열 포인터            | 2차원 배열 한 행 크기 명시 | `int (*arr)[3]`           |
| 포인터 배열            | 여러 주소 저장용        | `int *p[3];`              |

---

## 💬 오늘의 소감

오늘은 포인터를 함수 매개변수로 전달하는 다양한 방법을 익혔다.
특히 **배열과 포인터의 관계**를 함수 관점에서 다시 보니,
값 복사와 주소 전달의 차이가 한눈에 들어왔다.
