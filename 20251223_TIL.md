# 🗓️ 2025년 12월 23일 TIL - Java 추상 클래스(Abstract Class) & 인터페이스(Interface)


## 📌 오늘의 키워드

`추상 클래스(Abstract Class)` `추상 메서드(abstract method)` `상속(extends)`
`인터페이스(Interface)` `구현(implements)` `다중 구현` `다형성(Polymorphism)`
`업캐스팅(Interface 타입 참조)` `구현체(Implementation)`

---

## 1️⃣ 추상 클래스란?

### 💡 한 줄 정의

**“공통 뼈대(필드/생성자/일부 구현)는 부모가 제공하고, 꼭 필요한 핵심 기능은 자식이 ‘반드시’ 완성하도록 강제하는 클래스”**

오늘 코드에서 `Idol`은 **추상 클래스**이고, `introduce()`, `perform()`을 **추상 메서드**로 만들어서
자식(`SoloArtist`, `GroupIdol`)이 **반드시 구현하도록 강제**


---

## 2️⃣ 추상 메서드가 “강제”하는 것

`Idol`에 아래처럼 추상 메서드가 있으면:

* `public abstract void introduce();`
* `public abstract void perform();`

자식 클래스는 **무조건 오버라이딩(구현)**
안 하면 컴파일 에러가 나서 “미완성 클래스”로 남음

✅ 포인트

* 추상 클래스는 **객체 생성 불가** (new Idol() 불가)
* 대신, **자식 생성 시 부모 생성자(super)** 를 통해 공통 필드 초기화 가능

---

## 3️⃣ 인터페이스(Interface)란?

### 💡 한 줄 정의

**“어떤 역할(능력)을 할 수 있는지 ‘기능 목록’만 약속하는 설계도”**

오늘 코드에서:

* `Performer` : 공연할 수 있다(`perform`), 연습할 수 있다(`practice`)
* `Singer` : 노래할 수 있다(`sing`), 보컬 트레이닝 할 수 있다(`vocalTraining`)

`Actor`는 `implements Performer, Singer`로 **두 역할을 동시에 구현**
즉, **다중 구현**이 가능

📌 비유로 기억하기

* 인터페이스 = **자격증(역할)**

  * “가수 자격증(Singer)”
  * “공연자 자격증(Performer)”
* `Actor` = **자격증 2개를 가진 사람(역할 2개 가능)**

---

## 4️⃣ 추상 클래스 vs 인터페이스

| 구분        | 추상 클래스(Abstract Class)    | 인터페이스(Interface)        |
| --------- | ------------------------- | ----------------------- |
| 목적        | “같은 계열”의 공통 뼈대 제공 + 구현 강제 | “역할/능력”을 약속(기능 목록)      |
| 상속/구현     | `extends` (단일 상속)         | `implements` (다중 구현 가능) |
| 공통 상태(필드) | 가질 수 있음                   | 보통 상태를 안 둠(상수만 가능)      |
| 오늘 예시     | `Idol`                    | `Performer`, `Singer`   |

---

## 5️⃣ 다형성: “인터페이스 타입으로도 가능”

오늘 `Main`에서 핵심:

* `Performer actor1 = new Actor("김종국");`
* `Singer actor2 = new Actor("장혁");`

즉, **구현체(Actor)를 인터페이스 타입으로 참조(업캐스팅)** 가능

✅ 장점

* “역할 기준으로” 코드를 짤 수 있음(구현체가 바뀌어도 영향이 적음)

한계

* `Performer`로 받으면 `Singer` 기능(`sing`)은 바로 호출 못 함
  (그 역할 타입에 선언된 기능만 보임)

---

## 🎯 최종 정리

| 개념         | 한 줄 요약                              |
| ---------- | ----------------------------------- |
| 추상 클래스     | 공통 뼈대를 주고, 핵심 메서드는 자식이 반드시 완성하도록 강제 |
| 추상 메서드     | 구현이 없고, 자식이 무조건 오버라이딩해야 함           |
| 인터페이스      | “역할(기능 목록)”만 약속하는 설계도               |
| 다중 구현      | 클래스는 인터페이스를 여러 개 `implements` 가능    |
| 다형성(인터페이스) | 구현체를 인터페이스 타입으로 담아 역할 중심 코드 작성 가능   |

---

## 💻 실습 코드

### `Idol.java`

```java
package chap53.abs;

// 추상 클래스
public abstract class Idol {

    // 공통 속성
    protected String name;
    protected String groupName;

    // 부모의 생성자
    protected Idol() {
//        System.out.println("아이돌 부모 기본 객체 생성됨!");
    }

    protected Idol(String name, String groupName) {
        this.name = name;
        this.groupName = groupName;
//        System.out.println("아이돌 부모 객체 생성됨!");
    }

    // 공통 기능
    // 추상 메서드 - 자식 클래스가 반드시 구현해야 함
    public abstract void introduce();
    public abstract void perform();
}
```

### `SoloArtist.java`

```java
package chap53.abs;

public class SoloArtist extends Idol {

    String soloSong;

    // 생성자
    public SoloArtist(String name, String soloSong) {
        // 자식 생성자 첫 줄에서만 super() 호출 가능
        super(name, "솔로");
        this.soloSong = soloSong;
        System.out.println("솔로 가수가 생성됨!");
    }

    // 단독 기능
    public void singSolo() {
        System.out.printf("%s 님이 %s 노래를 부릅니다.\n"
            , this.name, this.soloSong);
    }

    // 아이돌이면 반드시 자기소개 기능과 공연하기 기능이 필수
    // 반드시 구현해야 하는 추상 메서드들, 구현하지 않으면 컴파일 에러
    @Override
    public void perform() {
        System.out.println(name
            + "이(가) 솔로 무대에서 특별한 공연을 합니다.");
    }

    @Override
    public void introduce() {

    }
}
```

### `GroupIdol.java`

```java
package chap53.abs;

public class GroupIdol extends Idol {

    int memberCount;

    // 생성자
    public GroupIdol(String name, String groupName, int memberCount) {
        super(name, groupName);
        this.memberCount = memberCount;
    }

    // 단독 기능
    public void groupDance() {
        System.out.printf("%s의 %d명이 함께 춤을 춥니다.\n"
            , this.groupName, this.memberCount);
    }


    // 아이돌이면 반드시 자기소개 기능과 공연하기 기능이 필수
    // 반드시 구현해야 하는 추상 메서드들, 구현하지 않으면 컴파일 에러
    @Override
    public void introduce() {

    }

    @Override
    public void perform() {
        System.out.println(name
            + "이(가) 그룹 무대에서 화려한 공연을 합니다.");
    }
}
```

### `Performer.java`

```java
package chap53.inter;

// 상속은 한개의 클래스만 가능, 인터페이스는 다중 상속 가능
// 객체 생성 불가, 필드는 자동으로 public static final이 됨, 즉 상수만 선언 가능
public interface Performer {

    // 공연기능
    public abstract void perform(); // public abstract는 안 붙여도 자동으로 붙음

    // 공연연습기능
    public abstract void practice();
}
```

### `Singer.java`

```java
package chap53.inter;

public interface Singer {

    void sing();
    void vocalTraining();
}
```

### `Actor.java`

```java
package chap53.inter;

// Performer를 구현, Actor는 구현체
// 상속과 구현 동시에 가능
public class Actor implements Performer, Singer {

    // 필드
    private String name;

    // 생성자
    public Actor(String name) {
        this.name = name;
    }

    // 인터페이스들의 추상 메서드를 구현
    @Override
    public void perform() {
        System.out.println(name + "이(가) 공연합니다.");
    }

    @Override
    public void practice() {
        System.out.println(name + "이(가) 연습합니다.");
    }

    @Override
    public void sing() {
        System.out.println(name + "이(가) 노래합니다.");
    }

    @Override
    public void vocalTraining() {
        System.out.println(name + "이(가) 노래 연습을 합니다.");
    }
}
```

### `Main.java`

```java
package chap53.inter;

public class Main {

    public static void main(String[] args) {

        // 다형성
        Performer actor1 = new Actor("김종국");
        Singer actor2 = new Actor("장혁");
        Actor actor3 = new Actor("아이유");

        actor1.perform();
        actor2.sing();
        actor3.vocalTraining();
    }
}
```

---

## 💬 오늘의 소감

* 추상 클래스는 “공통 기반(상태/생성자)” + “필수 동작 강제”로 계열 구조를 안정적으로 잡는 데 유리했다.
* 인터페이스는 “역할 중심 타입”으로 구현체 의존도를 낮춰 확장·교체에 유연한 설계를 가능하게 했다.
* `Performer actor = new Actor(...)` 같은 다형성이 유지보수성을 높이는 핵심이라는 점을 체감했다.
* 앞으로는 “공통 기반이 필요한가?” vs “역할만 분리하면 되는가?”를 먼저 판단해 추상 클래스/인터페이스를 선택하겠다.
