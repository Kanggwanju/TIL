# 🗓️ 2025년 12월 19일 TIL - Java 상속(Inheritance)

## 📌 오늘의 키워드

`상속(Inheritance)` `extends` `Is-a 관계` `protected` `super()`
`생성자 호출 순서` `오버라이딩(Override)` `동적 바인딩`
`this vs super` `필드 숨김(field hiding)` `생성자에서 오버라이딩 메서드 호출 주의`

---

## 1️⃣ 상속(Inheritance)이란?

### 💡 한 줄 정의

**이미 만들어진 클래스(부모)의 속성/기능을 “물려받아” 새 클래스(자식)를 만드는 방법**

* 공통 기능을 부모에 모아두면, 자식 클래스는 **재사용 + 확장**이 쉬워짐
* 예제에서는 `Idol`이 부모, `SoloArtist/GroupIdol/Actor`가 자식 구조

📌 비유로 기억하기

* 부모 클래스 = **기본 레시피(공통 규칙)**
* 자식 클래스 = **토핑/스타일 추가한 메뉴(확장)**
  → “기본은 같고, 일부만 다르게!”

---

## 2️⃣ `extends`와 Is-a 관계(“~은 ~이다”)

`extends`는 “자식이 부모를 상속한다”는 뜻

* `SoloArtist extends Idol` → “솔로 가수는 아이돌이다”
* `GroupIdol extends Idol` → “그룹 아이돌은 아이돌이다”

---

## 3️⃣ 상속할 때 필드는 `private` 말고 `protected`가 중요한 이유

부모의 공통 필드/메서드를 자식이 써야 하는데, `private`면 자식이 접근을 못 함.

그래서 예제에서는 부모 `Idol`의 공통 속성과 기능이 `protected`로 열려 있음:

* `protected String name, groupName`
* `protected void introduce(), perform()`

* `private` : “나만 봄”
* `protected` : “가족(상속받은 자식)까진 공유”

---

## 4️⃣ 생성자와 `super()` 규칙

### ✅ 핵심 규칙 1) **자식 생성자 시작하면, 부모 생성자가 먼저 실행됨**

`SoloArtist` 생성자에서 `super(name, "솔로")`를 가장 먼저 호출
`GroupIdol`도 마찬가지로 `super(name, groupName)` 호출
부모 `Idol(String, String)`은 “부모 객체 생성됨!”을 출력하도록 되어있음.

### ✅ 핵심 규칙 2) `super()` / `this()`는 **생성자 첫 줄에서만 가능**

### ✅ 핵심 규칙 3) 생성자 첫 줄에 `super()`/`this()`를 안 쓰면?

* 자동으로 `super()`가 삽입됨
* 부모 기본 생성자가 없으면 에러 가능


부모 `Idol`에는 기본 생성자(`protected Idol()`)가 있어서, `Actor`는 자동 `super()`가 가능

---

## 5️⃣ 오버라이딩(Override)

부모 `Idol.perform()`은 기본 공연
근데 자식은 각자 무대가 다르니까 `perform()`을 재정의(오버라이딩)

* `SoloArtist.perform()` : 솔로 무대 버전
* `GroupIdol.perform()` : 그룹 무대 버전

그리고 `Main`에서 `iu.perform()`, `wonyoung.perform()`을 호출하면 각 자식 버전이 실행된다.

---

## 6️⃣ `this` vs `super` + 필드 숨김(field hiding)

### ✅ Case1: 부모/자식에 같은 이름의 필드 `a`가 있을 때

* 부모 `Parent.a = 1`, 자식 `Child.a = 2`
* 생성자에서 `super.a = this.a = a`로 부모/자식 값을 동시에 세팅
* 출력에서 `this.a`(자식)와 `super.a`(부모)를 비교

포인트: 필드는 오버라이딩이 아니라 “숨김”이라서 `super.a`로 부모 필드를 따로 접근할 수 있다.

---

## 7️⃣ 실습 퀴즈 풀이 핵심

### ✅ Quiz1 (정답: `5,5,7`)

`set(5)`에서 `super.a`와 `this.a`를 둘 다 5로 만들고, `print(7)`은 (부모 a, 자식 a, 매개변수 a)를 출력

### ✅ Quiz2 (정답: `P(1)` → `C0`)

`Child2()`는 `super(1)`을 먼저 호출해서 부모 생성자 출력이 먼저 나오고, 그 다음에 `C0` 출력

### ✅ Quiz3 (정답: `C:0`)

부모 생성자에서 `print()`를 호출했는데, 실제 실행 주체는 **Child3의 오버라이딩된 print()**
그런데 그 순간엔 자식 필드 `v` 초기화가 덜 끝나서 `0`이 출력됨.

📌 결론 한 줄
**부모 생성자에서 오버라이딩 메서드 호출은 “초기화 순서” 때문에 위험할 수 있다.**

---

## 🎯 최종 정리

| 오늘 배운 것           | 한 줄 요약                          |
| ----------------- | ------------------------------- |
| `extends`         | “자식이 부모를 상속한다”                  |
| Is-a 관계           | “솔로/그룹은 아이돌이다”처럼 자연스러운 관계일 때 적합 |
| `protected`       | 상속 구조에서 “자식이 접근 가능한 공유 범위”      |
| `super()`         | 자식 생성자에서 부모 생성자 먼저 호출(첫 줄 규칙)   |
| 오버라이딩             | 같은 이름 메서드를 자식 상황에 맞게 재정의        |
| `this` vs `super` | 자식/부모 멤버를 구분해서 접근               |
| Quiz3 핵심          | 생성자에서 오버라이딩 메서드 호출은 초기화 순서 이슈   |

---

## 💻 실습 코드

### `Idol.java`

```java
package chap51.inherit;

// 상위 클래스 (super class)
// 부모의 속성이나 기능은 private을 사용하면 안된다. (protected 추천)
public class Idol {

    // 공통 속성
    protected String name;
    protected String groupName;

    // 부모의 생성자
    protected Idol() {
        System.out.println("아이돌 부모 기본 객체 생성됨!");
    }

    protected Idol(String name, String groupName) {
        this.name = name;
        this.groupName = groupName;
        System.out.println("아이돌 부모 객체 생성됨!");
    }

    // 공통 기능
    protected void introduce() {
        System.out.printf("안녕하세요! %s의 %s입니다.\n", groupName, name);
    }
    protected void perform() {
        System.out.println(name + "이(가) 무대에서 공연합니다.");
    }
}
```

### `SoloArtist.java`

```java
package chap51.inherit;

// 상속의 키워드는 extends
// 하위 클래스 (sub class)
// Idol과 Is a 관계, 그룹 아이돌은 아이돌이다.
public class SoloArtist extends Idol {

    String soloSong;

    // 생성자
    public SoloArtist(String name, String soloSong) {
        // 자식 생성자 첫 줄에서만 super() 호출 가능
        super(name, "솔로");
        this.soloSong = soloSong;
        System.out.println("솔로 가수가 생성됨!");
    }

    // 단독 기능
    public void singSolo() {
        System.out.printf("%s 님이 %s 노래를 부릅니다.\n"
                , this.name, this.soloSong);
    }

    // 오버라이딩: 자식 클래스에서 부모 클래스의 메서드를 재정의
    // 매개변수, 반환 타입, 메서드명은 부모와 동일
    @Override // 컴파일러 체크
    public void perform() { // 부모 메서드의 접근 제어자 수준보다 더 넓게 설정해야 함
        System.out.println(name
            + "이(가) 솔로 무대에서 특별한 공연을 합니다.");
    }
}
```

### `GroupIdol.java`

```java
package chap51.inherit;

// 상속의 키워드는 extends
// 하위 클래스 (sub class)
// Idol과 Is a 관계, 그룹 아이돌은 아이돌이다.
public class GroupIdol extends Idol {

    int memberCount;

    // 생성자
    public GroupIdol(String name, String groupName, int memberCount) {
        super(name, groupName);
        this.memberCount = memberCount;
    }

    // 단독 기능
    public void groupDance() {
        System.out.printf("%s의 %d명이 함께 춤을 춥니다.\n"
                , this.groupName, this.memberCount);
    }

    // 오버라이딩
    @Override
    public void perform() {
        System.out.println(name
            + "이(가) 그룹 무대에서 화려한 공연을 합니다.");
    }
}
```

### `Actor.java`

```java
package chap51.inherit;

public class Actor extends Idol {

    // 설계도 (클래스) 내부에 생성자를 만들지 않으면??
    // -> 기본 생성자가 생성됨
    public Actor(String name) {
        // 생성자의 첫줄에 this(), super()를 사용하지 않으면??
        // -> 자동으로 첫줄에 super()가 삽입됨!!
        // 하지만, 기본생성자(super())가 없으면 에러 발생.

        // -> 1. 부모에게 기본 생성자를 만들어주고 자동으로 super()가 삽입되어 에러 해결
        // super(); // 기본 생성자 만들었으면 생략 가능

        // -> 2. 부모에게 기본 생성자를 만들지 않고 다른 생성자를 호출해줌.
        // super(name, "배우");

        System.out.println("배우 객체가 생성됨!");
    }
}
```

### `Main.java`

```java
package chap51.inherit;

public class Main {

    public static void main(String[] args) {
        
        // 배우 생성
        new Actor("차은우");

        // 솔로 가수 생성
        SoloArtist iu = new SoloArtist("아이유", "좋은 날");

        iu.singSolo();
        iu.introduce();

        // 그룹 가수 생성
        GroupIdol wonyoung = new GroupIdol("장원영", "아이브", 6);

        wonyoung.groupDance();
        wonyoung.introduce();

        System.out.println("===================");
        iu.perform();
        wonyoung.perform();
    }
}
```

### `Case1.java`

```java
package chap51.exam;

class Parent {
    int a = 1;
}
class Child extends Parent {
    int a = 2;

    Child(int a) {
        // 부모 a와 자식 a를 모두 a로 설정
        super.a = this.a = a;
    }

    void print() {
        System.out.println("this.a = " + this.a);
        System.out.println("super.a = " + super.a);
    }
}

public class Case1 {

    public static void main(String[] args) {

        Child c = new Child(5);
        // 부모, 자식의 a 모두 5
        c.print();
    }
}
```

### `Quiz1.java`

```java
package chap51;

public class Quiz1 {
    public static void main(String[] args) {
        Child c = new Child();
        c.set(5);
        // 정답: 5,5,7
        c.print(7);
    }
}

class Parent {
    int a = 1;
}
class Child extends Parent {
    int a = 2;

    void set(int a) {
        super.a = this.a = a;
    }
    void print(int a) {
        System.out.println(super.a + "," + this.a + "," + a);
    }
}
```

### `Quiz2.java`

```java
package chap51;

public class Quiz2 {

    public static void main(String[] args) {
        // 문제: new Child2(); 코드 실행시 결과는?
        new Child2();
        /*
            정답:
            P(1)
            C0
         */
    }
}

class Parent2 {
    Parent2(int x) {
        System.out.println("P(" + x + ")");
    }
}

class Child2 extends Parent2 {
    Child2() {
        super(1);
        System.out.println("C0");
    }

    Child2(int x) {
        super(x);
        System.out.println("C1");
    }
}
```

### `Quiz3.java`

```java
package chap51;

public class Quiz3 {

    public static void main(String[] args) {
        // 정답: C:0
        new Child3();
    }
}

class Parent3 {
    Parent3() {
        // 부모 생성자에서 print를 했지만, 주체가 Child3 이므로
        // 메서드 호출시 자식 클래스의 오버라이딩 된 메서드가 실행된다.
        // Child 객체가 메모리에 생성되지 않았으므로, v=20이 아닌 v=0이 된다.
        print();
    }
    void print() {
        System.out.println("P:" + v);
    }
    int v = 10;
}

class Child3 extends Parent3 {
    int v = 20;
    Child3() {
        // super(); 생략됨
        v = 30;
    }

    @Override
    void print() {
        System.out.println("C:" + v);
    }
}
```

---

## 💬 오늘의 소감

* **생성자 순서(super 먼저)**가 핵심 포인트
* 특히 Quiz3는 “아… 이래서 생성자에서 오버라이딩 메서드 호출 조심하라 했구나”가 체감됐다.
* `protected`가 “상속 구조에서 왜 필요한지” `Idol` 예제로 확인할 수 있었다.
