# 🗓️ 2025년 11월 10일 TIL - c언어

## 📌 오늘의 키워드

`배열과 포인터` `포인터 연산` `문자열 포인터` `포인터 배열` `문자열 상수` `*(str + i)` `*str + 1`

---

## 1️⃣ 배열 이름과 포인터

### 개념 정리

* 배열의 이름(`arr`)은 사실상 **배열의 첫 번째 요소의 주소**를 의미한다.
* 따라서 `arr`과 `&arr[0]`은 같은 주소를 가리킨다.
* 하지만 `arr`은 상수 포인터처럼 동작하여 **다른 곳을 가리킬 수는 없다.**

### 예시 코드

```c
int arr[5] = {10, 20, 30, 40, 50};

printf("배열의 이름(arr)이 가진 주소: %p\n", arr);
printf("배열 0번 인덱스의 주소: %p\n", &arr[0]);
```

✅ 두 출력 결과는 동일하다.
배열 이름은 곧 **주소**이며, `*arr`은 `arr[0]`과 같다.

---

## 2️⃣ 포인터의 덧셈과 주소 이동

### 개념 정리

* 포인터에 정수를 더하면, 그 자료형의 크기만큼 주소가 증가한다.
* 예를 들어 `int`는 4바이트이므로 `arr + 1`은 `4`바이트 뒤의 주소를 가리킨다.

### 예시 코드

```c
int arr[5] = {10, 20, 30, 40, 50};

printf("arr[0]의 주소: %p\n", &arr[0]);
printf("arr[1]의 주소: %p\n", &arr[1]);
printf("arr의 주소: %p\n", arr);
printf("arr + 1의 주소: %p\n", arr + 1);
```

✅ `arr + 1`은 다음 요소의 주소를 나타내며, 4바이트씩 증가함을 확인할 수 있다.

---

## 3️⃣ 배열 인덱스와 포인터 연산의 관계

### 개념 정리

* `arr[i]`는 `*(arr + i)`와 완전히 동일한 표현이다.
* 배열의 요소 접근은 내부적으로 **포인터 연산**으로 처리된다.

### 예시 코드

```c
int arr[5] = {10, 20, 30, 40, 50};

printf("대괄호 방식 접근: %d\n", arr[3]);
printf("포인터 방식 접근: %d\n", *(arr + 3));
```

✅ `arr[3] == *(arr + 3)`
두 표현은 같은 메모리 주소를 참조한다.

---

## 4️⃣ 포인터를 사용한 배열 순회

### 개념 정리

* 배열의 모든 요소를 **포인터를 이동시키며 순회**할 수 있다.
* `*(ptr + i)`를 통해 각 요소의 값을 읽는다.

### 예시 코드

```c
int arr[5] = {10, 20, 30, 40, 50};
int *ptr = arr;

for (int i = 0; i < 5; i++) {
    printf("[%d] 위치의 값: %d\n", i, *(ptr + i));
}
```

✅ 포인터를 이용해 배열을 순회할 수 있으며,
`arr[i]`와 같은 결과를 얻는다.

---

## 5️⃣ 문자열과 포인터 이동

### 개념 정리

* 문자열은 사실 **문자 배열(char[])** 이며, 각 문자는 포인터를 이용해 순차적으로 접근 가능하다.
* `while(*p != '\0')`로 문자열의 끝을 검사한다.

### 예시 코드

```c
char str[] = "Hello";
char *p = str;

printf("문자열 값: ");
while (*p != '\0') {
    printf("%c", *p);
    p++;
}
```

✅ 포인터가 한 글자씩 이동하면서 문자열 전체를 출력한다.

---

## 6️⃣ 문자열 선언 방식의 차이

### 개념 정리

* `char str1[] = "Hello";`
  → **새로운 배열이 만들어지고 수정 가능**
* `char *str2 = "World";`
  → **읽기 전용 영역(문자열 상수)의 주소를 가리킴, 수정 불가**

### 예시 코드

```c
char str1[] = "Hello";
str1[0] = 'J';
printf("str1: %s\n", str1); // Jello

char *str2 = "World";
// str2[0] = 'Z';  // ❌ 런타임 오류 (읽기 전용 영역)
printf("str2: %s\n", str2);
```

✅ 배열 선언은 수정 가능, 포인터 선언은 읽기 전용 문자열을 가리킨다.

---

## 7️⃣ *(str + 1) 과 *str + 1의 차이

### 개념 정리

* `*(str + 1)` : **두 번째 문자**를 의미
* `*str + 1` : 첫 번째 문자의 ASCII 코드 값에 1을 더한 문자

### 예시 코드

```c
char str[] = "ACE";

printf("*(str + 1)의 결과: %c\n", *(str + 1)); // C
printf("*str + 1의 결과: %c\n", *str + 1);     // B
```

✅ 괄호 위치에 따라 의미가 전혀 달라짐.
포인터와 연산자의 결합 순서를 주의해야 한다.

---

## 8️⃣ 포인터 배열 (문자열 배열)

### 개념 정리

* 문자열 여러 개를 저장할 때 **포인터 배열**을 사용한다.
* 각 포인터가 서로 다른 문자열의 시작 주소를 가리킨다.

### 예시 코드

```c
char *fruits[3] = {"apple", "banana", "orange"};

for (int i = 0; i < 3; i++) {
    printf("과일: %s\n", fruits[i]);
}
```

✅ 각 인덱스가 **문자열의 시작 주소를 가리키는 포인터** 역할을 한다.

---

## 9️⃣ 포인터 배열 활용

### 개념 정리

* `char *p[2];` → 문자열 2개의 주소를 저장할 수 있는 포인터 배열
* `p[0] = s1;`, `p[1] = s2;` 형태로 문자열을 연결할 수 있다.
* 문자열 함수(`strlen`)과의 결합도 가능하다.
* `%s`: 널 문자가 나올 때까지 반복문으로 모든 글자 출력

### 예시 코드

```c
char *p[2];
char s1[] = "C language";
char s2[] = "is fun!";
p[0] = s1;
p[1] = s2;

printf("%s\n", p[0] + 2);        // language
printf("%zu\n", strlen(p[1]));   // 7
```

✅ 포인터 배열을 사용하면 다수의 문자열을 효율적으로 관리할 수 있다.

---

## 🎯 최종 정리

* 배열 이름은 배열의 시작 주소를 의미
* `arr[i]`와 `*(arr + i)`는 같은 표현
* 포인터는 자료형 크기 단위로 주소를 이동
* 문자열은 `char[]` 또는 `char*`로 선언할 수 있지만, 수정 가능 여부가 다르다.
* `*(str + i)`와 `*str + i`는 전혀 다른 연산
* 포인터 배열을 사용하면 여러 문자열을 효율적으로 관리할 수 있다.

---

## 💬 오늘의 소감

오늘은 포인터와 배열, 문자열의 관계를 중심으로 공부했다.
`arr`, `&arr[0]`, `*(arr + i)`처럼 모두 연결되어 있다는 점이 인상 깊었다.
특히 문자열 상수는 수정 불가하다는 점을 실제 오류 예시로 체감할 수 있었고,
포인터 배열을 활용하며 **문자열을 리스트처럼 다루는 구조적 사고**를 익힐 수 있었다.
