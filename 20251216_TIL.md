# 🗓️ 2025년 12월 16일 TIL - Java 배열(Array) & 반복문

## 📌 오늘의 키워드

`배열(Array)` `참조 타입(Reference Type)` `기본값(Default Value)` `length`
`for문` `향상된 for문(Enhanced for)` `2차원 배열(Matrix)` `가변 길이 배열(Jagged Array)` `swap(교환)`

---

## 1️⃣ 배열(Array) 기본 개념

### 💡 배열이 뭔가?

* **같은 타입의 값들을 연속된 공간에 묶어서** 다루는 자료구조
* 자바에서 배열 변수는 **값을 직접 들고 있는 게 아니라, 배열 객체의 주소(참조)** 를 들고 있는 **참조 변수**

#### ✅ 핵심 포인트

| 개념                   | 기억할 말                  |
| -------------------- | ---------------------- |
| 배열 변수 (`int[] arr`)  | **참조 변수** (배열 객체를 가리킴) |
| 배열 생성 (`new int[5]`) | 실제 **배열 객체 생성**        |
| 인덱스 (`arr[0]`)       | 0부터 시작, `0 ~ length-1` |
| 길이 (`arr.length`)    | 배열의 크기(요소 개수)          |

---

### 🔍 배열 출력이 이상하게 보이는 이유

* `System.out.println(arr)`를 찍으면 **배열 내용이 아니라 객체 식별 정보(해시코드 형태)** 가 나옴
* 배열 내용을 보고 싶으면 `Arrays.toString(arr)` 사용

---

## 2️⃣ 배열의 기본값(Default Value)

* `new int[4]`처럼 크기만 만들면, 자바는 **자동으로 기본값으로 채워줌**

| 타입              | 기본값     |
| --------------- | ------- |
| `int`           | `0`     |
| `double`        | `0.0`   |
| `boolean`       | `false` |
| `String`(참조 타입) | `null`  |

---

## 3️⃣ 배열 순회: for문 vs 향상된 for문

### 1) 일반 for문

* 인덱스가 필요할 때 유리 (`i`로 위치 제어 가능)

### 2) 향상된 for문 (Enhanced for)

* **값만 꺼내서** 반복할 때 간결함
* 단점: **인덱스를 직접 다루기 어려움** (몇 번째 요소인지가 중요하면 일반 for가 더 적합)

---

## 4️⃣ 2차원 배열(Matrix) 이해하기

* `int[][] arr = new int[2][3]` → **2행 3열**
* 순회할 때는 보통 **이중 for문**
* `arr.length`는 **행(row) 개수**, `arr[i].length`는 **i번째 행의 열(column) 개수**

### ✅ 가변 길이 2차원 배열(Jagged Array)

* `{{10,20}, {30,40,50}, {60}}`처럼 **행마다 길이가 다를 수 있음**
* 그래서 `arr[i].length`를 항상 확인하는 습관이 중요!

---

## 5️⃣ 미니 퀴즈로 정리한 핵심 패턴

| 문제           | 핵심 패턴                              |
| ------------ | ---------------------------------- |
| 짝수 합 구하기     | `if (x % 2 == 0) sum += x;`        |
| 문자열 개수 세기    | 문자열 비교는 `equals()`                 |
| 특정 행 길이/값 접근 | `scores[1].length`, `scores[2][0]` |
| swap(교환)     | `temp` 변수로 값 임시 저장                 |

---

## 🎯 최종 정리

| 오늘 배운 것               | 한 줄 요약                            |
| --------------------- | --------------------------------- |
| 배열은 참조 타입             | 배열 변수는 **주소를 들고** 배열 객체를 가리킴      |
| 기본값이 자동 세팅            | 크기만 만들면 타입에 따라 `0/0.0/false/null` |
| `length`는 필수          | 인덱스 범위 체크의 기준                     |
| 출력은 `Arrays.toString` | 그냥 출력하면 해시코드 형태                   |
| 반복문 선택 기준             | 인덱스 필요 → `for`, 값만 필요 → `향상된 for` |
| 2차원/가변 길이 배열          | `arr[i].length`로 열 개수 체크          |
| swap                  | `temp`로 안전하게 값 교환                 |

---

## 💻 실습 코드 (원본 그대로)

### `ArrayBasic.java`

```java
package chap47;

import java.util.Arrays;

public class ArrayBasic {

    public static void main(String[] args) {

        // 1. 배열 변수의 선언
        int[] arr; // 참조 변수, 주소 조작 불가능
        // 2. 배열 객체 생성
        arr = new int[5];

        System.out.println("arr: " + arr); // 해시코드

        // 3. 배열 초기화
        arr[0] = 99;
        arr[1] = arr[0] + 10;
        arr[2] = (int) 50.5;
        arr[3] = 5;
        arr[4] = arr[1] + arr[2];
//        arr[99] = 11;

        // 4. 배열 값 참조
        System.out.println("arr[0]의 값: " + arr[0]);

        // 5. 배열에 저장된 전체값 보기
        System.out.println(Arrays.toString(arr));

        // 6. 자바의 배열은 만들기만 하고 냅두면
        //    알아서 빈칸을 채운다.
        int[] intArr = new int[4];
        System.out.println(Arrays.toString(intArr));
        double[] dArr = new double[6];
        System.out.println(Arrays.toString(dArr));
        boolean[] bArr = new boolean[3];
        System.out.println(Arrays.toString(bArr));
        String[] sArr = new String[5];
        System.out.println(Arrays.toString(sArr));

        int[] numbers = {10, 20, 30, 40};
        System.out.println("numbers: " + Arrays.toString(numbers));
        System.out.println("numbers의 개수: " + numbers.length);

    }
}
```

### `ArrayLoop.java`

```java
package chap47;

public class ArrayLoop {

    public static void main(String[] args) {

        int[] scores = {95, 88, 100, 72, 67, 80};
        int total = 0;

        for (int i = 0; i < scores.length; i++) {
            System.out.printf("%d번째 점수: %d\n", i+1, scores[i]);
            total += scores[i];
        }
        System.out.println("총점: " + total);

        // 향상된 for문, 인덱스 번호 사용 불가
        for (int s : scores) {
            System.out.printf("점수: %d\n", s);
            total += s;
        }
        System.out.println("총점: " + total);

    }
}
```

### `Matrix.java`

```java
package chap47;

public class Matrix {

    public static void main(String[] args) {

        // 2x3 2차원배열
        int[][] arr = new int[2][3];
        
        arr[0][0] = 10;
        arr[1][2] = 60;
        
        // 순회
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr[i].length; j++) {
                System.out.printf("arr[%d][%d] = %d\n", i, j, arr[i][j]);
            }
            System.out.println(); // 줄바꿈
        }

        for (int[] is : arr) {
            for (int n : is) {
                System.out.print(n + " ");
            }
            System.out.println();
        }
    }
}
```

### `Quiz1.java`

```java
package chap47;

// 실습 퀴즈 1
public class Quiz1 {

    public static void main(String[] args) {

        int[] nums = {12, 17, 8, 41, 30, 22};
        int sum = 0;
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] % 2 == 0) {
                sum += nums[i];
            }
        }

        // 정답 72
        System.out.println(sum);
    }
}
```

### `Quiz2.java`

```java
package chap47;

// 실습 퀴즈 2
public class Quiz2 {

    public static void main(String[] args) {

        String[] fruits = {"apple", "banana", "apple", "mango", "apple"};
        int count = 0;
        
        // 향상된 for 문
        for (String fruit : fruits) {
            // 문자열을 비교할 때 equals 사용
            if (fruit.equals("apple")) {
                count++;
            }
        }

        // 정답: 3
        System.out.println(count);
    }
}
```

### `Quiz3.java`

```java
package chap47;

// 실습 퀴즈 3
public class Quiz3 {

    public static void main(String[] args) {

        int[][] scores = {{10, 20}, {30, 40, 50}, {60}};
        int result = 0;

        result += scores[1].length; // 3
        result += scores[2][0]; // 60

        // 정답 63
        System.out.println(result);
    }
}
```

### `Quiz4.java`

```java
package chap47;

// 실습 퀴즈 4
public class Quiz4 {

    public static void main(String[] args) {

        int[] arr = {10, 20, 30, 40, 50};
        int temp;

        temp = arr[1];
        arr[1] = arr[3];
        arr[3] = temp;

        // 정답: 40 + 20 = 60
        System.out.println(arr[1] + arr[3]);
    }
}
```

---

## 💬 오늘의 소감

* 배열은 “여러 값을 묶는 도구”인데, 자바에서는 특히 **참조 타입**이라는 점이 중요했다.
* `Arrays.toString()` / `length` / 반복문 선택 기준만 잘 잡아도 배열 관련 실수가 확 줄어들 것 같다.
* 2차원 배열은 `arr.length`(행)과 `arr[i].length`(열)을 분리해서 생각하니까 헷갈림이 많이 줄었다.
